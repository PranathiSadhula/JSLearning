console.log('Write a JS program to print any mathematics tables based on the input value');
 var input = 1;
 for (let index = 1; index <= 10; index++) {
     console.log(input + '*' + index  +'=' +input * index );
     
 }